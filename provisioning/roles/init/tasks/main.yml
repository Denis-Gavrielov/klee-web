---

# - name: Upgrade packages
#   apt:
#     update_cache: yes
#     upgrade: yes

- name: Update and install git
  apt:
    name:
      - git
      # - language-pack-en-base # TODO: do we really need this?
    update_cache: yes
    state: latest

- name: Update and install language-pack-en-base
  apt:
    name:
      - language-pack-en-base
    update_cache: yes
    state: latest
  when: not ci